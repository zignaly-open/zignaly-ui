/*import { useState } from "react";

const [list, setToastList] = useState([]);

const showToast = (type: string) => {
  let toastProperties = "";
  if (type === "success") {
    toastProperties = "success";
  } else if (type === "error") {
    toastProperties = "error";
  } else {
    setToastList([]);
  }
  setToastList([...list, toastProperties]);
};

showToast("success");*/
